# ─────────────────────────────────────────────────────────────────────────────
# DANA KIGALI HOTEL — environment variable template
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in real values.
# .env is listed in .gitignore — never commit credentials.
#
# ⚠️  IMPORTANT: These are temporary client-side credentials.
#     Replace the login() function body in AuthContext.jsx with
#     a real backend API call before production deployment.
# ─────────────────────────────────────────────────────────────────────────────

VITE_ADMIN_EMAIL=your-admin@email.com
VITE_ADMIN_PASSWORD=your-secure-password
